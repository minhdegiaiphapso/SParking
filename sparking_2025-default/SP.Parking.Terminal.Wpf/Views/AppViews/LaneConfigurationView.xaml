<Views:BaseView x:Class="SP.Parking.Terminal.Wpf.Views.AppViews.LaneConfigurationView"
             xmlns:Views="clr-namespace:SP.Parking.Terminal.Wpf.Views"
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:common="clr-namespace:SP.Parking.Terminal.Wpf.Views.Common"
                xmlns:w="clr-namespace:SP.Parking.Terminal.Wpf.Utility"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >

    <Views:BaseView.Resources>
        <w:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
        <w:StringCodeToString x:Key="StringCodeToString" />
        <w:EnumToFriendlyNameConverter x:Key="enumItemsConverter"/>
    </Views:BaseView.Resources>
    
    <ScrollViewer>
        <Grid Name="MainGrid" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.5" />
                <ColumnDefinition Width="10*" />
                <ColumnDefinition Width="0.5" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" MinHeight="400"/>
                <RowDefinition Height="*" MinHeight="400"/>
                <RowDefinition Height="*" MinHeight="400"/>
                <RowDefinition Height="*" MinHeight="400"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>
            <!--<GroupBox Header="Cấu hình chung" Margin="10" Grid.Row="2"
                      Grid.Column="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="0.1*"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <common:StreachableTextBlock TextAlignment="Right" Margin="8,8,16,8" Grid.Column="0" Grid.Row="0" TBContent="Camera port" HorizontalAlignment="Right"/>
                    <TextBox Margin="0,5,0,5" Grid.Column="1" FontWeight="Bold" FontSize="16" TextAlignment="Center" Foreground="DarkBlue"
                             VerticalAlignment="Center" HorizontalAlignment="Stretch" Text="{Binding Path=CameraPort, Mode=TwoWay}" ></TextBox>
                    <Grid Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3"
                          MinHeight="64">
                        <Button  Style="{DynamicResource AccentedSquareButtonStyle}" Content="Lưu thay đổi" HorizontalAlignment="Center" VerticalAlignment="Center"
                                 Width="120"
                                 Height="30"
                                 Margin="5" Command="{Binding SaveCommand}" />
                        <TextBlock Grid.Row="9" Grid.Column="1" Grid.ColumnSpan="3"
                                   HorizontalAlignment ="Center"
                                   VerticalAlignment="Bottom" 
                                   FontSize="14"
                                   TextWrapping="NoWrap"
                                   TextAlignment="Center"
                                   FontWeight="Bold" 
                                   Margin="0,7,0,0"
                                   Width="400" Text="{Binding SaveResultMessage, Converter={StaticResource StringCodeToString}}"
                                   Foreground="DarkBlue"/>
                    </Grid>
                </Grid>
            </GroupBox>-->
        </Grid>
    </ScrollViewer>
    
</Views:BaseView>
