<Views:BaseView x:Class="SP.Parking.Terminal.Wpf.Views.AppViews.VoucherView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:Views="clr-namespace:SP.Parking.Terminal.Wpf.Views"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
      xmlns:w="clr-namespace:SP.Parking.Terminal.Wpf.Utility"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="4500" KeyUp="BaseView_KeyUp">

    <!--<UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Icons.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>-->
    <Views:BaseView.Resources>
        <w:BoolVisiblilityConverter x:Key="BoolVisibility" />
        <w:StringCodeToString x:Key="StringCodeToString" />
    </Views:BaseView.Resources>
    <Grid x:Name="grdmain" >
        <Grid.RowDefinitions>
            <RowDefinition Height="60"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <Label>Tiền phí</Label>
                <TextBlock Margin="5 0 5 0" Background="Red" FontSize="18" Foreground="White" TextAlignment="Center" x:Name="lblFee"></TextBlock>
            </StackPanel>
            <StackPanel Grid.Column="1">
                <Label>Khấu trừ</Label>
                <TextBlock Margin="5 0 5 0" Background="Red" FontSize="18" TextAlignment="Center" Foreground="White" x:Name="lblVoucherAmount"></TextBlock>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <Label>Phải trả</Label>
                <TextBlock  Margin="5 0 5 0" Background="Red" TextAlignment="Center" FontSize="18" Foreground="White" x:Name="lblActualFee"></TextBlock>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="25"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="45"></RowDefinition>
            </Grid.RowDefinitions>
            <WrapPanel Grid.Row="2" VerticalAlignment="Center">
                <Button VerticalContentAlignment="Center" Margin="20 0 20 0" Width="100" Click="btnSave_Click" Background="LawnGreen" x:Name="btnSave" ToolTip="Bấm phím S">Lưu</Button>
                <Button VerticalContentAlignment="Center" Margin="20 0 20 0" Width="100" Background="Gray" Click="btnCancel_Click"  x:Name="btnCancel" ToolTip="Bấm phím C">Bỏ qua</Button>

            </WrapPanel>
            <WrapPanel Grid.Row="0">
                <RadioButton Margin="3" Checked="RadioButton_Checked" IsChecked="True" GroupName="vouchertype">Voucher theo giờ</RadioButton>
                <RadioButton Margin="3" Checked="RadioButton_Checked_1" IsChecked="False" GroupName="vouchertype">Voucher Miễn phí</RadioButton>
                <RadioButton Visibility="Hidden" Margin="3" Checked="RadioButton_Checked_2" IsChecked="False" GroupName="vouchertype">Voucher sự kiện</RadioButton>
            </WrapPanel>
            <Grid x:Name="Byhour" Grid.Row="1" Visibility="Visible">
                <Grid.RowDefinitions>
                    <RowDefinition Height="45"></RowDefinition>
                    <RowDefinition Height="45"></RowDefinition>
                </Grid.RowDefinitions>
                
                <WrapPanel Grid.Row="0" Margin="5">
                    <Label Width="120">Số Voucher 4h</Label>
                    <TextBox  MaxLength="2"  TextChanged="txt4_TextChanged" Width="150" x:Name="txt4" PreviewTextInput="txt4_PreviewTextInput"></TextBox>
                </WrapPanel>
                <WrapPanel Grid.Row="1" Margin="5">
                    <Label Width="120">Số Voucher 24h</Label>
                    <TextBox MaxLength="2" TextChanged="txt24_TextChanged" Width="150" x:Name="txt24" PreviewTextInput="txt24_PreviewTextInput"></TextBox>
                </WrapPanel>
            </Grid>
            <Grid x:Name="FOC" Grid.Row="1" Visibility="Hidden">

            </Grid>
            <Grid x:Name="byevent" Grid.Row="1" Visibility="Hidden">
                <WrapPanel Grid.Row="1" Margin="5">
                    <Label Width="120">Phải trả</Label>
                    <TextBox TextChanged="txtMoney_TextChanged" Width="150" x:Name="txtMoney" PreviewTextInput="txtMoney_PreviewTextInput"></TextBox>
                </WrapPanel>
            </Grid>
        </Grid>
    </Grid>
</Views:BaseView>