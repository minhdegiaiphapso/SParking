<UserControl x:Class="SP.Parking.Terminal.Wpf.Views.Common.StreachableTextBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:Name="uc" >
    <Grid>
        <Viewbox HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
            <TextBlock x:Name="TextBlockChild" DataContext="{Binding ElementName=uc}" 
                       VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="{Binding TextAlignment}" 
                       TextWrapping="WrapWithOverflow" MinWidth="150" Margin="0" Text="{Binding Path=TBContent, Mode=TwoWay}"/>
        </Viewbox>
    </Grid>
</UserControl>
