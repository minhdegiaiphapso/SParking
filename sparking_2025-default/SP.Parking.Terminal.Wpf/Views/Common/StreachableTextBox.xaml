<UserControl x:Class="SP.Parking.Terminal.Wpf.Views.Common.StreachableTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:Name="uc" >
    <Grid>
        <Viewbox HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
            <TextBox Name="TextBoxChild" TextWrapping="WrapWithOverflow" Margin="0" BorderThickness="0" TextAlignment="Center" HorizontalAlignment="Stretch" 
                     FontSize="20"
                     CaretBrush="Transparent" Text="{Binding Path=TBContent, UpdateSourceTrigger=PropertyChanged, ElementName=uc}" />
        </Viewbox>
    </Grid>
</UserControl>
