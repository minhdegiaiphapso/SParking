<controls:MetroWindow x:Class="SP.Parking.Terminal.Wpf.UI.AboutView"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                        xmlns:w="clr-namespace:SP.Parking.Terminal.Wpf.Utility"
                        WindowStyle="SingleBorderWindow"
                        Width="450"
                        Height="558"
                        Background="White"
                        Title="ABOUT"
                        WindowStartupLocation="CenterOwner"
                        ResizeMode="NoResize"
                        ShowInTaskbar="False"
                        BorderThickness="1"
                        BorderBrush="{StaticResource VietPark.Brushes.WayOut}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Image Opacity="1" Grid.Column="1" Grid.Row="0" Source="/Images/capture_view_wayout.png" Margin="20" 
                           MaxHeight="200"  MaxWidth="200" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Grid Grid.Row="1">
            <Separator Background="{StaticResource VietPark.Brushes.WayOut}"/>
        </Grid>
        <StackPanel HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Grid.Row="2"
                    Margin="4">
            <TextBlock Text="Version: 1.0.2"
                       Name="tblVersion" 
                       Foreground="Gray"/>
            <TextBlock Name="tblGuId" 
                       Text="Guid"
                       Foreground="Gray"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Name="tblStorage" 
                           VerticalAlignment="Center"
                       Text="Log storage: "
                           Foreground="Gray"/>
                <Button Margin="5,0,0,0"
                        FontSize="12"
                        FontWeight="Normal"
                        VerticalAlignment="Center"
                        Content="CLEAR"
                        Click="Button_Click" Cursor="Hand" Foreground="White"/>
            </StackPanel>
            
        </StackPanel>
        
        <StackPanel Orientation="Vertical"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Grid.Row="2">
            <TextBlock Name="tblSite"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Bottom"
                   Margin="0,0,6,0"
                       Foreground="Gray">
                <TextBlock>Website:</TextBlock>
                <Hyperlink Foreground="Gray" NavigateUri="http://vietpark.tech/" RequestNavigate="Hyperlink_RequestNavigate">vietpark.tech</Hyperlink>
            </TextBlock>
            <TextBlock Name="tblEmail"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Bottom"
                   Margin="0,4,6,4"
                       Foreground="Gray">
                <TextBlock>Email:</TextBlock>
                <Hyperlink Foreground="Gray" NavigateUri="contact@vietpark.tech" RequestNavigate="Hyperlink_RequestNavigate_1">contact@vietpark.tech</Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</controls:MetroWindow>
