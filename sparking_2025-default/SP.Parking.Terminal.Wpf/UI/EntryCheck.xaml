<myui:CentralLaneWindow x:Class="SP.Parking.Terminal.Wpf.UI.EntryCheck"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                        xmlns:myui="clr-namespace:SP.Parking.Terminal.Wpf.UI"
                        xmlns:w="clr-namespace:SP.Parking.Terminal.Wpf.Utility"
                        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                        WindowStyle="SingleBorderWindow"
                         mc:Ignorable="d" 
                         Height="348" Width="502.03">
    <StackPanel DataContext="{Binding CheckInData}">
        <!-- Vehicle Number -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0">Biển số:</TextBlock>
            <TextBlock Grid.Column="1" Text="{Binding AlprVehicleNumber, Mode=TwoWay}"></TextBlock>
        </Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0">Số lần:</TextBlock>
            <TextBlock Grid.Column="1" Text="{Binding EntryCount}"></TextBlock>
        </Grid>
        <Grid Height="280">
            <DataGrid Margin="0" ItemsSource="{Binding Entries}" 
                              AutoGenerateColumns="False" 
                              Name="EntriesGrid"
                              HorizontalAlignment="Stretch" 
                              VerticalAlignment="Stretch"
                              Grid.Column="0"
                              SelectedItem="{Binding Path=SelectedItem, Mode=TwoWay}"
                              RenderOptions.ClearTypeHint="Enabled"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              TextOptions.TextFormattingMode="Display">
                <DataGrid.CellStyle>
                    <Style TargetType="{x:Type DataGridCell}" BasedOn="{StaticResource MetroDataGridCell}">
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="Red" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="{x:Type DataGridRow}">
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentColorBrush3}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Check In" Width="*"
                                                Binding="{Binding StrCheckInTime}" 
                                                IsReadOnly="True"/>
                    <DataGridTextColumn Header="Check Out" Width="*"
                                                Binding="{Binding StrCheckOutTime}" 
                                                IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </StackPanel>
</myui:CentralLaneWindow>
